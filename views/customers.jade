doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    //block content
    h1= title       
    ul 
        each val in values 
            li='Name: ' + val.name + (val.id ? (", id: " + val.id) : "")